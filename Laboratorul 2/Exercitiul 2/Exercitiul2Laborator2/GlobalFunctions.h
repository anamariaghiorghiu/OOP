#pragma once
#include "Student.h"

int CompareNames(Student, Student);
int CompareMath(Student, Student);
int CompareHistory(Student, Student);
int CompareEnglish(Student, Student);
int CompareAverage(Student, Student);